name: one-staff-prod

services:
  app:
    build:
      context: .
      target: runner
      args:
        # Hardcoded for Docker - must use host.docker.internal, not 127.0.0.1
        - NEXT_PUBLIC_SUPABASE_URL=http://host.docker.internal:5101
        - NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=${NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY}
        - NEXT_PUBLIC_SITE_URL=http://localhost:5102
    ports:
      - "5102:3000"
    env_file:
      - .env.local
    environment:
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:3000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5
