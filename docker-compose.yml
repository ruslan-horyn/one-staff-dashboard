name: one-staff-dev

services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "5100:3000"
    env_file:
      - .env.local
    environment:
      # Override URL for Docker networking (container can't reach 127.0.0.1)
      - NEXT_PUBLIC_SUPABASE_URL=http://host.docker.internal:5101
      - NEXT_PUBLIC_SITE_URL=http://localhost:5100
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    extra_hosts:
      - "host.docker.internal:host-gateway"
